import React from 'react';
import './header.css';

const Header = () => {

    return(
        <header className="header">
            <span>Foodiepy</span>
        </header>
    );
};

export default Header;